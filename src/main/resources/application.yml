server:
  port: 9092

spring:
  main:
    banner-mode: off

  datasource:
    initialization-mode: always
    url: jdbc:postgresql://postgres:5432/medialab
    username: medialab
    password: medialab

  jpa:
    hibernate:
      ddl-auto: create-drop

    open-in-view: false